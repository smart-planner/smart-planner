import Login from './subpages/Login/'
import Planner from './subpages/Planner/'

export const routes = [
    {
        path: '/Login',
        component: Login
    },
    {
        path: '/',
        component: Planner
    }
]